# Realtime Order Medallion — dependencies (single place for install)
#
# Install everything:  pip install -r requirements.txt
# Then: make install  (or run setup.sh) for script permissions and Java check.
#
# Not in pip:
#   - Java 11+ (for Spark): brew install openjdk@17; set JAVA_HOME. See docs/SETUP_AND_RUN.md §4.
#   - Spark Kafka connector: in config/pipeline.yaml (spark_packages); used by scripts/run_bronze.sh.

# --- Core: Spark & pipeline ---
pyspark>=3.4.0
confluent-kafka>=2.3.0
pyyaml>=6.0

# --- Optional: Postgres (sink / CDC) ---
psycopg2-binary>=2.9.0

# --- Hotel ordering website (make web) ---
flask>=3.0.0

# --- Testing ---
pytest>=7.0.0
