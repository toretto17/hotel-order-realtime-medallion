# Cron setup on the VM

**Use the main doc:** All steps (including creating `cron_run.sh` if it’s missing and verifying cron) are in **docs/PIPELINE_IN_CLOUD_FREE.md** → **Step 17** and **Step 18**.

Follow that document step-by-step so setup is reproducible and trackable. A full runbook (start to end) is planned once the project is complete — see README and docs/NEXT_STEPS_AND_FUTURE_GOALS.md.
